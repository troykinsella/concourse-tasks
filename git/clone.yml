---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: troykinsella/concourse-util

inputs:
- name: tasks

outputs:
- name: git-repo

run:
  path: tasks/git/clone.sh
  args:
  - git-repo
